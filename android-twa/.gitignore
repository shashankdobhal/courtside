# Never commit the signing keystore.
*.keystore
*.jks

# Generated by `bubblewrap init` — the actual Android Gradle project.
# Regenerate it locally rather than versioning it; only twa-manifest.json
# (the input) is tracked here.
/app/
/gradle/
/gradlew
/gradlew.bat
/build.gradle
/settings.gradle
/*.aab
/*.apk
